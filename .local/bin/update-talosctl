#!/usr/bin/env bash

VERSION=$1

if [[ $VERSION == '' ]]
then
  echo 'Usage: update-talosctl <version>'
  exit 1
fi

curl -L \
  https://github.com/siderolabs/talos/releases/download/v${VERSION}/talosctl-linux-arm64 \
  -o ~/.local/bin/talosctl

chmod 755 ~/.local/bin/talosctl
